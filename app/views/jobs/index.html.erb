<div class="col-md-12">
  <div class="job">
    <%= link_to("Add a job", new_group_path, class: "btn btn-primary pull-right") %>
  </div>
  <table class="table table-hover">
    <thead>
      <tr>
        <td>#</td>
        <td>Title</td>
        <td>description</td>
      </tr>
    </thead>
    <tbody>
      <% @jobs.each do |group| %>
      <tr>
        <td>#</td>
        <td><%= link_to(job.title, group_path(job)) %></td>
        <td><%= job.description %></td>
        <td>
          <%= link_to("Edit", edit_group_path(job), class: "btn btn-smbtn-default")%>
          <%= link_to("Destroy", group_path(job))>, class: "btn btn-smbtn-default", method: :delete, data: { confirm: "Are you sure?"})%>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
